diff --git a/node_modules/vite-plugin-sentry/dist/index.js b/node_modules/vite-plugin-sentry/dist/index.js
index 1ef25c1..bfc9c57 100644
--- a/node_modules/vite-plugin-sentry/dist/index.js
+++ b/node_modules/vite-plugin-sentry/dist/index.js
@@ -175,10 +175,10 @@ function ViteSentry(options) {
         */
         configResolved(config) {
             pluginState.sourcemapsCreated = !!config.build.sourcemap;
-            pluginState.isProduction = config.isProduction;
+            pluginState.isProduction = config.mode === 'production';
             pluginState.enabled =
                 pluginState.sourcemapsCreated &&
-                    (skipEnvironmentCheck || config.isProduction);
+                    (skipEnvironmentCheck || pluginState.isProduction);
         },
         /*
           Resolve id for virtual module
diff --git a/node_modules/vite-plugin-sentry/dist/index.mjs b/node_modules/vite-plugin-sentry/dist/index.mjs
index 4d4ff6b..373e285 100644
--- a/node_modules/vite-plugin-sentry/dist/index.mjs
+++ b/node_modules/vite-plugin-sentry/dist/index.mjs
@@ -169,10 +169,10 @@ function ViteSentry(options) {
         */
         configResolved(config) {
             pluginState.sourcemapsCreated = !!config.build.sourcemap;
-            pluginState.isProduction = config.isProduction;
+            pluginState.isProduction = config.mode === 'production';
             pluginState.enabled =
                 pluginState.sourcemapsCreated &&
-                    (skipEnvironmentCheck || config.isProduction);
+                    (skipEnvironmentCheck || pluginState.isProduction);
         },
         /*
           Resolve id for virtual module
