export const WHITELISTED_HELIS = ['prago'].map(m => GetHashKey(m));

export const MAX_FOV = 80.0;
export const MIN_FOV = 3.0;
export const ZOOM_SPEED = 4.0;
export const SPEED_X = 7.0;
export const SPEED_Y = 7.0;
